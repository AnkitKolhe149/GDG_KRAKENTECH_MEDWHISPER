{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "framework": "flask",
  "env": {
    "PYTHON_VERSION": "3.13"
  },
  "functions": {
    "run.py": {
      "memory": 3008,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/run.py"
    }
  ],
  "regions": ["iad1"],
  "installCommand": "pip install -r requirements.txt"
}
